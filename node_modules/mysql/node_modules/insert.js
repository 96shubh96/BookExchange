var mysql=require('mysql');

function DB(){
var connection =mysql.createConnection({
     host:'localhost',
	 user:'root',
	 password:'1996',
	 database:'articles'
});
connection.connect();
return connection;
}
