CREATE TABLE user_info (
 roll_no INT(11) PRIMARY KEY,
 name    VARCHAR(30) NOT NULL,
 branch  VARCHAR(30) NOT NULL,
 semester INT NOT NULL,
 contact  VARCHAR(20) NOT NULL,
 emailid  VARCHAR(30) NOT NULL,
 password VARCHAR(30) NOT NULL,
 record_created DATE NOT NULL,
  record_updated DATE NOT NULL
  
  );
  
 CREATE TABLE book_info(
 b_no INT(11) Not null AUTO_INCREMENT,
 bookname Varchar(50) NOT NULL,
 author Varchar(50) NOT NULL,
 edition varchar(10) NOT NULL,
 primary key(b_no)
 );
 Insert into book_info set bookname='Computer Networks',author='Andrew S.Tanenbaum',edition='5th'; 
 Insert into book_info set bookname='Computer Graphics',author='Donald Heran  M.Pauline Baker',edition='2nd'; 
 
 Create table relation(
 roll_no INT(11) PRIMARY KEY,
 name    VARCHAR(30) NOT NULL,
 bookname Varchar(50) NOT NULL,
 edition varchar(10) NOT NULL
 );